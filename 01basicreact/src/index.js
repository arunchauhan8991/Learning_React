
import ReactDOM from 'react-dom/client'; 
//react-dom is implementation of react on web, like react native is implementation of react on mobile app.

import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
 
    <App />
  
);

